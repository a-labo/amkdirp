/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

'use strict'

const amkdirp = require('./amkdirp')

let lib = amkdirp.bind(this)

Object.assign(lib, amkdirp, {
  amkdirp
})

module.exports = lib
